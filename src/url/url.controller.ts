// import { Controller, Body, Post, Request } from '@nestjs/common';
// import { UrlService } from './url.service';
// import { CreateUrlDto } from './interfaces/dto/create-url.dto';
// import { JwtPayload } from '../auth/interfaces/jwt.interface';

// // import { Request } from 'express';

// @Controller('url')
export class UrlController {
  //   constructor(private readonly urlService: UrlService) {}
  //   @Post()
  //   async create(@Body() body: CreateUrlDto, @Request() req: JwtPayload) {
  //     const {
  //       user: { id: ownerId },
  //     } = req || null;
  //     const baseUrl = `${req.protocol}://${req.get('host')}`;
  //     return await this.urlService.create(body.originalUrl, ownerId, baseUrl);
  //   }
}
